#importando a biblioteca Random
import random

num1 = random.random() #o metodo random na classe random gera um numero aleatório entre 0 e 1 (float)
#para gerar um número inteiro de 1 a 10 seria random.randint(1, 10)
num2 = random.randint(1, 10)
print(num1, ', ', num2)

#se vc digitar import e der ctrl+espaço, vc tem acesso a lista de importações possiveis.
#acessando python.org, clicando em PyPI, vc tem acesso ao package index, q é uma lista de pacotes
#extras.

#de exemplo, temos a biblioteca emoji, que serve apenas para mostrar emojis
#Arquivo Aula 08c
